import React from 'react'

import Header from './Header'

export default (props) => {
  return (
    <div>
      plop2 from app in component apiauth8 client

      <Header />

      { props.children }

    </div>
  )
}